# setMethod ('split', signature=signature(x='TimeIntervalDataFrame', f='TimeInstantDataFrame', drop='ANY'),
# 	   definition=function(x, f, drop, ..., boundaries=c('include', 'exclude', 'cut'), keep.f=TRUE)
# 	   {
# 	   } )
# 
# setMethod ('split', signature=signature(x='TimeIntervalDataFrame', f='SubtimeDataFrame', drop='ANY'),
# 	   definition=function(x, f, drop, ..., boundaries=c('include', 'exclude', 'cut'), keep.f=TRUE)
# 	   {
# 	   } )
# 
# setMethod ('split', signature=c(x='TimeIntervalDataFrame', f='subtime', drop='ANY'),
# 	   definition=function(x, f, boundaries=c('include', 'exclude', 'cut'), keep.f=TRUE, drop, ...)
# 	   {
# 	   } )
# 
# setMethod ('split', signature=c(x='TimeIntervalDataFrame', f='TimeIntervalDataFrame', drop='ANY'),
# 	   definition=function(x, f, boundaries=c('include', 'exclude', 'cut'), keep.f=TRUE, drop, ...)
# 	   {
# 	   } )
# 
# setMethod ('split', signature=c(x='TimeIntervalDataFrame', f='POSIXcti', drop='ANY'),
# 	   definition=function(x, f, boundaries=c('include', 'exclude', 'cut'), keep.f=TRUE, drop, ...)
# 	   {
# 	   } )
# 
# setMethod ('split', signature=c(x='TimeIntervalDataFrame', f='POSIXctp', drop='ANY'),
# 	   definition=function(x, f, boundaries=c('include', 'exclude', 'cut'), keep.f=TRUE, drop, ...)
# 	   {
# 	   } )
# 
# setMethod ('split', signature=c(x='TimeIntervalDataFrame', f='character', drop='ANY'),
# 	   definition=function(x, f, boundaries=c('include', 'exclude', 'cut'), keep.f=TRUE, drop, ...)
# 	   {
# 	   } )
# 
